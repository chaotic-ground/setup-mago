---
# Strict yamllint (upstream default); line-length and truthy.check-keys relaxed for GitHub Actions.
extends: default

# Lint only our own files; skip whatever git ignores (.tf/.terraform/).
ignore-from-file: .gitignore

rules:
  line-length:
    max: 120
  truthy:
    check-keys: false
